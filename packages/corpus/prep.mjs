// Moved to @inkpipe/imaging so the corpus harness and the production agent run
// the identical preparation. Kept as a re-export for the spike scripts.
export { prepForModel, prepForVault, cropRegion } from '@inkpipe/imaging';
