# Normalise line endings so Windows and Linux checkouts match.
# This repo targets both, so a CRLF drift would show up as phantom diffs.
* text=auto eol=lf

*.sh text eol=lf
*.ps1 text eol=crlf

*.png binary
*.jpg binary
*.webp binary
